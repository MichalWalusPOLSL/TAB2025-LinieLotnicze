package com.project.demo.repository;

import model.AirfieldEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AirfieldRepository extends JpaRepository<AirfieldEntity, Long> {
}
